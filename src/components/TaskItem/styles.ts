import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#262626",
    borderWidth: 1,
    borderColor: "#333333",
    marginBottom: 8,
    borderRadius: 8,
    padding: 12,
  },
  name: {
    flex: 1,
    fontSize: 14,
    marginHorizontal: 8,
  },
});
